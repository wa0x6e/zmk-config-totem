include:
  - board: seeeduino_xiao_ble
    shield: totem_left
  - board: seeeduino_xiao_ble
    shield: totem_right
  # Uncomment to build a settings_reset firmware (clears stale BT bonds).
  # Flash it to one half, then flash the normal firmware back.
  # - board: seeeduino_xiao_ble
  #   shield: settings_reset
