CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
# 2M PHY left at Zephyr default (on) for lower latency/battery.
# If BLE starts dropping/stuttering, re-add CONFIG_BT_CTLR_PHY_2M=n to force 1M PHY (more reliable at range).
